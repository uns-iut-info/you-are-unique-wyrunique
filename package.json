{
  "name": "wyrunique",
  "version": "1.0.0",

  "main": "app.js",

  "dependencies": {
    "express": "^4.18.1"
  },
  "engines": {
    "node": "^16.14.0"
  },
  "scripts": {
    "deploy": "npm install",
    "start": "npm install --prefix public/ && node app.js"
  }
}
